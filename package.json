{
  "name": "discord-bot",
  "version": "0.0.0",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "vitest": "~3.2.0",
    "wrangler": "^4.105.0"
  },
  "dependencies": {
    "discord-interactions": "^4.4.0",
    "itty-router": "^5.0.24",
    "tweetnacl": "^1.0.3"
  }
}
